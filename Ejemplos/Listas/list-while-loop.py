fruitList = ["apple", "banana", "cherry"]

i = 0		               # initializing a variable that will be used as the loop counter and as List index

# while i <= len(fruitList) - 1:
while i < len(fruitList):

    print(fruitList[i])     # using i as the index, to reference the List element in the current iteration
    i += 1		            # incrementing the loop counter variable